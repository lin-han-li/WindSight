# 部署与终端上报协议

本仓库包含两个彼此独立的服务：

| 项目 | 目录 | 默认本地端口 | 定位 |
| --- | --- | ---: | --- |
| 风力发电机演示版 | `wind-turbine-demo/` | 8080 | 固定风机节点、固定四指标协议 |
| 万物数驱通用数据平台 | `wanwu-data-platform/` | 8080 | 任意 JSON、动态字段与单位 |

> 两个项目需使用**不同的端口、数据库文件、环境变量文件和服务名称**，不能共用同一运行目录。

---

## 1. 本地启动

以下示例使用 Windows PowerShell 和 Python 3.11。

### 1.1 风力发电机演示版

```powershell
cd wind-turbine-demo\server
py -3.11 -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
Copy-Item .\windsight.env.example .\windsight.env
# 编辑 windsight.env：至少设置 SECRET_KEY 和管理员密码
python .\app.py
```

默认访问：`http://127.0.0.1:8080`

### 1.2 万物数驱通用数据平台

```powershell
cd wanwu-data-platform\server
py -3.11 -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
Copy-Item .\windsight.env.example .\windsight.env
# 编辑 windsight.env：至少设置 SECRET_KEY 和管理员密码
python .\app.py
```

默认访问：`http://127.0.0.1:8080`

如果同一台电脑同时运行两个项目，请把其中一个项目的 `PORT` 改为 `8081`，并将其数据库 URL 改为不同文件。

---

## 2. 服务器部署原则

生产部署建议由 Nginx 反向代理到应用本地端口。每个项目独立创建：

```text
/opt/<project>/current/server       源码
/opt/<project>/venv                 Python 虚拟环境
/etc/<project>/<project>.env        私有环境变量
/var/lib/<project>/                 SQLite 数据库
/var/log/<project>/                 日志
/etc/systemd/system/<project>.service
/etc/nginx/conf.d/<project>.conf
```

### 必需配置

环境变量文件至少包含：

```ini
SECRET_KEY=<固定的随机值>
WINDSIGHT_CREDENTIAL_ENCRYPTION_KEY=<固定的Fernet密钥>
DATABASE_URL=sqlite:////var/lib/<project>/<project>.db
PORT=<仅本机监听的端口>
ALLOWED_ORIGINS=https://<你的域名>
WINDSIGHT_DEFAULT_ADMIN_USERNAME=admin
WINDSIGHT_DEFAULT_ADMIN_PASSWORD=<强密码>
NODE_TIMEOUT_SECONDS=90
```

- 运行时使用单个 Eventlet/Gunicorn worker；
- 应用本身监听 `127.0.0.1`，公网只开放 Nginx 的 80/443；
- 不要把 `.env`、`windsight.env`、数据库、日志、虚拟环境或 SSH 配置提交到 Git；
- 生产中 `SECRET_KEY` 与 `WINDSIGHT_CREDENTIAL_ENCRYPTION_KEY` 必须长期固定，否则 HMAC 凭据会失效。

万物数驱的 Alibaba Cloud Linux 3 参考模板位于：

```text
wanwu-data-platform/deploy/aliyun/
```

---

## 3. 终端节点上传认证

两个项目默认要求终端节点先在 Web 页面中注册。注册后保存页面显示的以下任一种凭据：

1. **节点密钥**：HTTP 头 `X-WindSight-Node-Key`；适合教学和调试；
2. **HMAC 凭据**：推荐生产使用，包含 `key_id` 和 `secret`。

所有上传均使用：

```text
POST /api/upload
Content-Type: application/json
```

节点 ID 只能包含大写字母、数字、下划线和短横线，长度为 3–64 位，例如：`WIND_001`、`LAB_SENSOR_01`。

---

## 4. 风力发电机演示版协议

风电版使用固定帧格式。每一帧必须包含：

- `node_id`：已注册节点 ID；
- `sub`：本帧风机数量，范围 `1..200`；
- 三位风机编号：`001` 到 `200` 内的绝对编号；
- 每台风机的 4 路原始模拟量：`[电压传感器V, 电流传感器V, 转速传感器V, 温度传感器V]`，每个值范围 `0..5V`。

```json
{
  "node_id": "WIND_001",
  "sub": "4",
  "001": [3.50, 2.00, 2.00, 1.60],
  "002": [3.52, 2.02, 2.03, 1.62],
  "003": [3.54, 2.04, 2.06, 1.64],
  "004": [3.56, 2.06, 2.09, 1.66]
}
```

映射关系：

| 原始输入 | 页面/数据库工程量 |
| --- | --- |
| 电压 `0..5V` | `0..250V` |
| 电流 `0..5V` | `0..5A` |
| 转速 `0..5V` | `0..2500 r/min` |
| 温度 `0..5V` | `0..100℃` |

风机编号可以按分包绝对编号上报，例如一帧上报 `031` 至 `036`；但 `sub` 必须等于本帧风机键数量。

用节点密钥测试：

```powershell
curl.exe -X POST "http://127.0.0.1:8080/api/upload" `
  -H "Content-Type: application/json" `
  -H "X-WindSight-Node-Key: <节点密钥>" `
  --data-binary "@wind-payload.json"
```

风电演示版自带模拟器：

```powershell
python .\sim.py --server http://127.0.0.1:8080 --node-id WIND_001 --sub 4 --node-key "<节点密钥>" --interval-ms 1000
```

---

## 5. 万物数驱通用 JSON 协议

万物数驱的顶层必须是 JSON 对象，并包含一个已注册来源标识。识别优先级：

1. `node_id`
2. `device_id`
3. `source_id`

除来源标识外，字段数量、嵌套结构和数据类型不受限制。有限数值自动形成可选择的曲线字段；文本、布尔和 `null` 保存在原始 JSON 中但不绘图。

```json
{
  "device_id": "LAB_SENSOR_01",
  "telemetry_schema": {
    "environment.temperature": {
      "label": "环境温度",
      "unit": "℃",
      "semantic": "temperature"
    },
    "environment.humidity": {
      "label": "相对湿度",
      "unit": "%RH",
      "semantic": "humidity"
    },
    "power.voltage": {
      "label": "供电电压",
      "unit": "V",
      "semantic": "voltage"
    }
  },
  "environment": {
    "temperature": 24.8,
    "humidity": 56.2
  },
  "power": {
    "voltage": 12.1
  },
  "status": "normal"
}
```

上述 JSON 自动得到：

```text
environment.temperature
environment.humidity
power.voltage
```

`telemetry_schema` 由上传端控制字段显示名、单位和语义。建议每一帧都携带它，以便设备重启或字段更新后仍保持统一展示。

用节点密钥测试：

```powershell
curl.exe -X POST "http://127.0.0.1:8080/api/upload" `
  -H "Content-Type: application/json" `
  -H "X-WindSight-Node-Key: <节点密钥>" `
  --data-binary "@sensor-payload.json"
```

实时页默认以四宫格显示已选字段前四项；总字段数不受限制，用户可在页面选择其他字段。历史页可按来源、字段和时间范围回放。

---

## 6. HMAC 上传头

生产终端推荐使用 HMAC-SHA256。服务端注册节点后会提供 `key_id` 与 `secret`。签名原文按以下各行组成：

```text
WIND-SIGHT-HMAC-SHA256
v1
POST
/api/upload
DEVICE_ID
KEY_ID
TIMESTAMP
NONCE
BODY_SHA256
```

签名算法为：

```text
HMAC-SHA256(secret, canonical_string)
```

请求头：

```text
X-WindSight-Signature-Version: v1
X-WindSight-Algorithm: HMAC-SHA256
X-WindSight-Device-Id: DEVICE_ID
X-WindSight-Key-Id: KEY_ID
X-WindSight-Timestamp: Unix 时间戳或 ISO UTC 时间
X-WindSight-Nonce: 每次请求唯一随机值
X-WindSight-Body-SHA256: 原始请求体的 SHA-256 十六进制值
X-WindSight-Signature: HMAC-SHA256 十六进制签名
```

时间戳必须处在服务器允许时间窗内，Nonce 不能重复。

---

## 7. 验证

两个项目均可在各自 `server/` 目录运行：

```powershell
python -m unittest discover -s tests -v
```

测试覆盖账户、权限、注册节点、上传认证、Socket.IO、协议校验和数据查询。
