# 分支说明

当前页面属于 `codex/two-projects-release`。本仓库同时保留原始 WindSight 工程和整理后的双项目目录。两个分支用途不同，切换分支只会改变当前工作区的文件组织方式，不会删除另一分支的提交。

## 当前公开分支

| 分支 | 适用场景 | 顶层内容 |
| --- | --- | --- |
| [`main`](https://github.com/lin-han-li/WindSight/tree/main) | 需要原始工程、原有本地路径或配套模拟器 | `code/`、`mini_simulator/`、`mini_server/`、`mini_one_node/`、`database/`、`ALiYunFuWuQi/` 等原始目录 |
| [`codex/two-projects-release`](https://github.com/lin-han-li/WindSight/tree/codex/two-projects-release) | 需要分别获取和运行两个独立项目 | `wind-turbine-demo/` 与 `wanwu-data-platform/`，以及各自说明和部署材料 |

## 如何选择

- 要保持原来的本地文件路径、使用原有模拟器或继续原工程开发：选择 `main`。
- 要单独运行风电项目：选择 `codex/two-projects-release` 中的 `wind-turbine-demo/`。
- 要接入任意节点数据并由上传端定义字段语义和单位：选择 `codex/two-projects-release` 中的 `wanwu-data-platform/`。

## 获取方式

直接获取原始工程（默认分支）：

```bash
git clone https://github.com/lin-han-li/WindSight.git
cd WindSight
# 当前默认就是 main
```

直接获取双项目目录：

```bash
git clone --branch codex/two-projects-release --single-branch https://github.com/lin-han-li/WindSight.git
cd WindSight
```

已克隆仓库时切换分支：

```bash
git fetch origin
git switch main
# 或
git switch --track origin/codex/two-projects-release
```

## 说明文件位置

- 原始工程：根目录的 `项目全面介绍.md`、`MINI_子项目总览.md`，以及各子目录 README。
- 双项目目录：根目录 `README.md`、`部署与终端节点上传协议.md`，以及两个项目目录中的 `使用说明.md`。

两个分支共用同一个 GitHub 仓库，但不应把一个分支的路径当成另一个分支的路径。